const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const taxes = await prisma.invoiceItemTax.findMany({ where: { invoiceItem: { invoice: { status: { in: ['SENT', 'PAID'] } } } }, select: { taxableAmount: true, taxRate: true, taxAmount: true, taxSystem: { select: { name: true, taxType: true, rate: true } }, invoiceItem: { select: { invoice: { select: { id: true, currency: true, exchangeRate: true } } } } } }); console.log(JSON.stringify(taxes, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
