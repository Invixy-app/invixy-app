const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const gte = new Date('2026-03-01T00:00:00Z'); const lte = new Date(); const taxLineItems = await prisma.invoiceItemTax.findMany({ where: { invoiceItem: { invoice: { businessId: 'cmkmdt2lt0000l204g4fc768g', status: { in: ['SENT', 'PAID'] }, issueDate: { gte, lte } } } }, select: { taxableAmount: true, taxRate: true, taxAmount: true, taxSystem: { select: { name: true, taxType: true, rate: true } }, invoiceItem: { select: { invoice: { select: { id: true, currency: true, exchangeRate: true } } } }, } }); console.log(JSON.stringify(taxLineItems, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
