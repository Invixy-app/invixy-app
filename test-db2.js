const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const taxes = await prisma.invoiceItemTax.findMany({ include: { invoiceItem: { include: { invoice: true } }, taxSystem: true } }); console.log(JSON.stringify(taxes.map(t => ({id: t.id, taxSystem: t.taxSystem, invoiceStatus: t.invoiceItem.invoice.status, invoiceDate: t.invoiceItem.invoice.issueDate})), null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
